/** Non-static parent for Next.js `<Image fill />` (Framer Motion-safe). */
export const fillImageParentStyle = { position: "relative" as const };

export const fillImageParentAbsoluteStyle = { position: "absolute" as const };
